# Đợt 2 — Hạ tầng i18n + login toggle + popup ngôn ngữ Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ngôn ngữ app trở thành có thật: cookie `n400_lang` + cột `n400_user_profile.ui_language`, trang login song ngữ VI/EN với toggle, popup chọn ngôn ngữ lần đầu, select Ngôn ngữ trong Profile có tác dụng ngay.

**Architecture:** Không next-intl. `src/lib/n400/i18n/` chứa config + dictionary typed (vi là nguồn shape, en phải khớp key) + server helpers + server action `setN400Language` (ghi cookie, và ghi DB nếu đã login) + client provider. Root layout `<html lang>` theo cookie. Middleware sync DB→cookie trong query profile-gate sẵn có. Spec: `docs/superpowers/specs/2026-07-17-n400ready-url-i18n-design.md` §4–§7.

**Tech Stack:** Next.js 16 App Router (đọc `node_modules/next/dist/docs/` khi cần — AGENTS.md cảnh báo API có thể khác training data), Supabase (`@supabase/ssr`), vitest.

**Working directory:** `apps/website`. Nhánh mới `feat/n400ready-i18n` (worktree). Mỗi Task = 1 commit atomic, commit ngay khi task xanh.

**Phạm vi dịch của Đợt 2:** CHỈ trang login + popup + toggle. Toàn bộ phần còn lại của app giữ nguyên tiếng Việt (Đợt 3–4).

---

### Task 1: Migration `ui_language`

**Files:**
- Create: `supabase/migrations/n400_14_ui_language.sql`

- [ ] **Step 1: Viết migration**

```sql
-- N400 app UI language. NULL = user has never chosen -> first-login popup.
-- Intentionally NOT on shared `profiles` (its preferred_language is
-- DEFAULT 'en' NOT NULL and belongs to internal_app/portal semantics).
ALTER TABLE n400_user_profile
  ADD COLUMN IF NOT EXISTS ui_language text
  CONSTRAINT n400_user_profile_ui_language_check
  CHECK (ui_language IN ('vi', 'en'));
```

- [ ] **Step 2: Apply lên Supabase**

CONTROLLER APPLIES (MCP `apply_migration`, name `n400_14_ui_language`). Implementer chỉ tạo file và báo controller. Verify sau apply: query `SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='n400_user_profile' AND column_name='ui_language';` → 1 row, `is_nullable=YES`.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/n400_14_ui_language.sql
git commit -m "feat(n400): add n400_user_profile.ui_language for app UI language"
```

---

### Task 2: i18n core — config, dictionaries, server helpers, action, provider

**Files:**
- Create: `src/lib/n400/i18n/config.ts`
- Create: `src/lib/n400/i18n/vi.ts`
- Create: `src/lib/n400/i18n/en.ts`
- Create: `src/lib/n400/i18n/server.ts`
- Create: `src/lib/n400/i18n/actions.ts`
- Create: `src/lib/n400/i18n/provider.tsx`
- Test: `src/lib/n400/i18n/i18n.test.ts`

- [ ] **Step 1: Failing test — key parity + config guards**

```ts
// src/lib/n400/i18n/i18n.test.ts
import { describe, expect, it } from 'vitest';
import { DEFAULT_N400_LANG, N400_LANGUAGES, isN400Lang } from './config';
import { vi as viDict } from './vi';
import { en as enDict } from './en';

type Tree = { [k: string]: string | Tree };

function keyPaths(obj: Tree, prefix = ''): string[] {
  return Object.entries(obj).flatMap(([k, v]) =>
    typeof v === 'string' ? [`${prefix}${k}`] : keyPaths(v as Tree, `${prefix}${k}.`)
  );
}

describe('n400 i18n config', () => {
  it('vi là mặc định và nằm trong danh sách', () => {
    expect(DEFAULT_N400_LANG).toBe('vi');
    expect(N400_LANGUAGES).toContain('vi');
    expect(N400_LANGUAGES).toContain('en');
  });

  it('isN400Lang chỉ nhận giá trị hợp lệ', () => {
    expect(isN400Lang('vi')).toBe(true);
    expect(isN400Lang('en')).toBe(true);
    expect(isN400Lang('es')).toBe(false);
    expect(isN400Lang('')).toBe(false);
    expect(isN400Lang(undefined)).toBe(false);
  });
});

describe('n400 dictionaries', () => {
  it('en có đúng tập key của vi (không thiếu, không thừa)', () => {
    expect(keyPaths(enDict as unknown as Tree).sort()).toEqual(
      keyPaths(viDict as unknown as Tree).sort()
    );
  });

  it('không có giá trị rỗng', () => {
    for (const dict of [viDict, enDict]) {
      for (const path of keyPaths(dict as unknown as Tree)) {
        const val = path.split('.').reduce<unknown>((o, k) => (o as Tree)[k], dict);
        expect(val, path).toBeTruthy();
      }
    }
  });
});
```

- [ ] **Step 2: Run để thấy RED** — `npx vitest run src/lib/n400/i18n` → FAIL (module not found).

- [ ] **Step 3: `config.ts`**

```ts
// Central language registry for the N400 app. Adding a language later
// (e.g. 'es') = add here + extend the DB CHECK + add its dictionary file.
export const N400_LANGUAGES = ['vi', 'en'] as const;
export type N400Lang = (typeof N400_LANGUAGES)[number];
export const DEFAULT_N400_LANG: N400Lang = 'vi';
export const N400_LANG_COOKIE = 'n400_lang';
export const N400_LANG_COOKIE_MAX_AGE = 60 * 60 * 24 * 365; // 1 year

export function isN400Lang(value: unknown): value is N400Lang {
  return typeof value === 'string' && (N400_LANGUAGES as readonly string[]).includes(value);
}
```

- [ ] **Step 4: `vi.ts` (nguồn shape)**

```ts
// Vietnamese dictionary — the source of truth for the dictionary shape.
// Đợt 2 covers ONLY the login screen + language modal. Later phases add
// namespaces per screen (dashboard, study, practice, ...).
export const vi = {
  login: {
    tagline: 'TỰ TIN CHINH PHỤC\nGIẤC MƠ MỸ!',
    headline1: 'Học thông minh.',
    headline2: 'Đậu phỏng vấn.',
    headline3: 'Trở thành công dân Mỹ.',
    heroSub:
      'N400 Ready là ứng dụng giúp bạn học và luyện thi quốc tịch Mỹ (N-400) một cách hiệu quả, dễ dàng và thú vị.',
    mobileHeroSub:
      'Ứng dụng giúp bạn học và luyện thi quốc tịch Mỹ (N-400) hiệu quả, dễ dàng và thú vị.',
    socialProof:
      'Được xây dựng từ trải nghiệm phỏng vấn quốc tịch Mỹ thực tế và hướng dẫn chính thức mới nhất của USCIS.',
    welcomePrefix: 'Welcome to',
    cardSubtitle: 'Tiếp tục hành trình chinh phục quốc tịch Mỹ 👋',
    continueWith: 'Tiếp tục với',
    or: 'HOẶC',
    continueWithEmail: 'Tiếp tục với Email',
    emailPlaceholder: 'Email',
    passwordPlaceholder: 'Mật khẩu',
    signingIn: 'Đang đăng nhập...',
    signIn: 'Đăng nhập',
    forgotPassword: 'Quên mật khẩu?',
    createAccount: 'Tạo tài khoản',
    loginFailed: 'Đăng nhập thất bại. Vui lòng thử lại.',
    securityNote1: 'Dữ liệu của bạn được bảo mật tuyệt đối.',
    securityNote2: 'Chúng tôi không chia sẻ thông tin của bạn.',
  },
  langModal: {
    title: 'Chọn ngôn ngữ hiển thị',
    subtitle: 'Choose your display language',
    vietnamese: 'Tiếng Việt',
    english: 'English',
    confirm: 'Xác nhận / Confirm',
    hint: 'Bạn có thể đổi lại bất cứ lúc nào trong phần Tài khoản.',
  },
};

export type N400Dict = typeof vi;
```

(KHÔNG dùng `as const` — để value có type `string`, nhờ đó `en: N400Dict` gán thẳng được mà vẫn bị ép đúng cấu trúc key.)

- [ ] **Step 5: `en.ts` (typed theo shape của vi — thiếu key là lỗi compile)**

```ts
import type { N400Dict } from './vi';

// Typed against the vi shape — a missing or extra key fails compile.
export const en: N400Dict = {
  login: {
    tagline: 'CONFIDENTLY CONQUER\nTHE AMERICAN DREAM!',
    headline1: 'Study smart.',
    headline2: 'Pass the interview.',
    headline3: 'Become a U.S. citizen.',
    heroSub:
      'N400 Ready helps you study and practice for the U.S. citizenship test (N-400) effectively, easily, and enjoyably.',
    mobileHeroSub:
      'The app that helps you study and practice for the U.S. citizenship test (N-400) effectively and enjoyably.',
    socialProof:
      'Built from real U.S. citizenship interview experience and the latest official USCIS guidance.',
    welcomePrefix: 'Welcome to',
    cardSubtitle: 'Continue your journey to U.S. citizenship 👋',
    continueWith: 'Continue with',
    or: 'OR',
    continueWithEmail: 'Continue with Email',
    emailPlaceholder: 'Email',
    passwordPlaceholder: 'Password',
    signingIn: 'Signing in...',
    signIn: 'Sign in',
    forgotPassword: 'Forgot password?',
    createAccount: 'Create account',
    loginFailed: 'Sign-in failed. Please try again.',
    securityNote1: 'Your data is fully protected.',
    securityNote2: 'We never share your information.',
  },
  langModal: {
    title: 'Chọn ngôn ngữ hiển thị',
    subtitle: 'Choose your display language',
    vietnamese: 'Tiếng Việt',
    english: 'English',
    confirm: 'Xác nhận / Confirm',
    hint: 'You can change this anytime in Account settings.',
  },
};
```

(langModal cố ý song ngữ tĩnh ở cả 2 dict — popup hiện TRƯỚC khi user chọn ngôn ngữ.)

- [ ] **Step 6: `server.ts`**

```ts
import 'server-only';
import { cookies } from 'next/headers';
import { DEFAULT_N400_LANG, N400_LANG_COOKIE, isN400Lang, type N400Lang } from './config';
import { vi, type N400Dict } from './vi';
import { en } from './en';

const DICTS: Record<N400Lang, N400Dict> = { vi, en };

/** Read the app language from the request cookie. No cookie -> 'vi'. */
export async function getN400Lang(): Promise<N400Lang> {
  const store = await cookies();
  const raw = store.get(N400_LANG_COOKIE)?.value;
  return isN400Lang(raw) ? raw : DEFAULT_N400_LANG;
}

export function getN400Dict(lang: N400Lang): N400Dict {
  return DICTS[lang];
}
```

(LƯU Ý: kiểm tra trong `node_modules/next/dist/docs/` xem `cookies()` bản Next này là sync hay async — chỉnh `await` cho khớp. Pattern chuẩn có sẵn trong `src/app/n400ready/(app)/setup/actions.ts`.)

- [ ] **Step 7: `actions.ts` — server action dùng chung cho toggle/popup/profile**

```ts
'use server';

import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';
import {
  N400_LANG_COOKIE,
  N400_LANG_COOKIE_MAX_AGE,
  isN400Lang,
} from './config';

/**
 * Persist the app language: always the cookie; also the DB row when a
 * user is signed in (popup/profile). Logged-out (login-page toggle) the
 * cookie alone is enough.
 */
export async function setN400Language(lang: string): Promise<{ ok: boolean }> {
  if (!isN400Lang(lang)) return { ok: false };

  const store = await cookies();
  store.set(N400_LANG_COOKIE, lang, {
    path: '/',
    maxAge: N400_LANG_COOKIE_MAX_AGE,
    sameSite: 'lax',
  });

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return store.getAll();
        },
        setAll() {
          /* auth cookie refresh not needed in this short-lived action */
        },
      },
    }
  );

  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (user) {
    await supabase
      .from('n400_user_profile')
      .update({ ui_language: lang })
      .eq('user_id', user.id);
  }

  return { ok: true };
}
```

- [ ] **Step 8: `provider.tsx`**

```tsx
'use client';

import { createContext, useContext, type ReactNode } from 'react';
import type { N400Lang } from './config';
import type { N400Dict } from './vi';

const N400LangContext = createContext<{ lang: N400Lang; dict: N400Dict } | null>(null);

export function N400LangProvider({
  lang,
  dict,
  children,
}: {
  lang: N400Lang;
  dict: N400Dict;
  children: ReactNode;
}) {
  return <N400LangContext.Provider value={{ lang, dict }}>{children}</N400LangContext.Provider>;
}

export function useN400Lang(): { lang: N400Lang; dict: N400Dict } {
  const ctx = useContext(N400LangContext);
  if (!ctx) throw new Error('useN400Lang must be used within N400LangProvider');
  return ctx;
}
```

- [ ] **Step 9: GREEN + commit**

`npx vitest run src/lib/n400/i18n` → PASS. `npx tsc --noEmit` → sạch.

```bash
git add src/lib/n400/i18n
git commit -m "feat(n400): i18n core — lang config, vi/en dictionaries, cookie action, provider"
```

---

### Task 3: `<html lang>` theo cookie + middleware sync DB→cookie

**Files:**
- Modify: `src/app/n400ready/layout.tsx`
- Modify: `src/middleware.ts`

- [ ] **Step 1: Root layout đọc cookie**

Trong `src/app/n400ready/layout.tsx`: import `getN400Lang` từ `@/lib/n400/i18n/server`; component thành `async function`; thay `lang="vi"` bằng `lang={lang}` với `const lang = await getN400Lang();`. Cập nhật comment (bỏ câu "Đợt 2 makes it follow the cookie").

- [ ] **Step 2: Middleware sync**

Trong `src/middleware.ts`, khối profile-gate hiện tại:

```ts
    const { data: n400Profile } = await supabase
      .from('n400_user_profile')
      .select('user_id')
      .eq('user_id', user.id)
      .maybeSingle();
    if (!n400Profile) {
      return NextResponse.redirect(new URL('/n400ready/setup', request.url));
    }
```

thành:

```ts
    const { data: n400Profile } = await supabase
      .from('n400_user_profile')
      .select('user_id, ui_language')
      .eq('user_id', user.id)
      .maybeSingle();
    if (!n400Profile) {
      return NextResponse.redirect(new URL('/n400ready/setup', request.url));
    }
    // DB is the source of truth once chosen — heal the cookie (new device,
    // cleared cookies). Takes effect from the next request; the action that
    // changes language sets the cookie directly so in-app switches are instant.
    const dbLang = n400Profile.ui_language;
    if (
      (dbLang === 'vi' || dbLang === 'en') &&
      request.cookies.get('n400_lang')?.value !== dbLang
    ) {
      supabaseResponse.cookies.set('n400_lang', dbLang, {
        path: '/',
        maxAge: 60 * 60 * 24 * 365,
        sameSite: 'lax',
      });
    }
```

(Middleware là Edge — import từ `i18n/config` được vì file thuần hằng số; nếu muốn tránh import chain, dùng literal như trên. Chọn literal cho an toàn Edge bundle.)

- [ ] **Step 3: Verify + commit**

`npx tsc --noEmit`, `npx eslint src/middleware.ts src/app/n400ready/layout.tsx` sạch.

```bash
git add src/app/n400ready/layout.tsx src/middleware.ts
git commit -m "feat(n400): html lang + cookie heal follow ui_language"
```

---

### Task 4: Trang login song ngữ + toggle VI/EN

**Files:**
- Create: `src/app/n400ready/(auth)/login/LangToggle.tsx`
- Modify: `src/app/n400ready/(auth)/layout.tsx`
- Modify: `src/app/n400ready/(auth)/login/page.tsx`
- Modify: `src/app/n400ready/(auth)/login/login.module.css` (thêm class cho toggle)

- [ ] **Step 1: (auth) layout bọc provider**

`src/app/n400ready/(auth)/layout.tsx` hiện là layout 9 dòng. Thành:

```tsx
import type { ReactNode } from 'react';
import { getN400Dict, getN400Lang } from '@/lib/n400/i18n/server';
import { N400LangProvider } from '@/lib/n400/i18n/provider';

export default async function N400AuthLayout({ children }: { children: ReactNode }) {
  const lang = await getN400Lang();
  return (
    <N400LangProvider lang={lang} dict={getN400Dict(lang)}>
      {children}
    </N400LangProvider>
  );
}
```

(Giữ nguyên mọi phần tử khác file cũ đang có — đọc file trước, chỉ thêm provider bọc quanh children hiện tại.)

- [ ] **Step 2: `LangToggle.tsx`**

```tsx
'use client';

import { useTransition } from 'react';
import { useRouter } from 'next/navigation';
import { N400_LANGUAGES, type N400Lang } from '@/lib/n400/i18n/config';
import { setN400Language } from '@/lib/n400/i18n/actions';
import { useN400Lang } from '@/lib/n400/i18n/provider';
import styles from './login.module.css';

const LABELS: Record<N400Lang, string> = { vi: 'VI', en: 'EN' };

/** VI/EN pill toggle on the login card. Cookie-only (logged out). */
export function LangToggle() {
  const { lang } = useN400Lang();
  const router = useRouter();
  const [pending, startTransition] = useTransition();

  function choose(next: N400Lang) {
    if (next === lang || pending) return;
    startTransition(async () => {
      await setN400Language(next);
      router.refresh();
    });
  }

  return (
    <div className={styles.langToggle} role="group" aria-label="Language">
      {N400_LANGUAGES.map((code) => (
        <button
          key={code}
          type="button"
          onClick={() => choose(code)}
          disabled={pending}
          className={code === lang ? styles.langToggleActive : styles.langToggleBtn}
          aria-pressed={code === lang}
        >
          {LABELS[code]}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: CSS toggle** — thêm vào cuối `login.module.css`:

```css
/* ─── Language toggle (top-right of login card) ─── */
.langToggle {
  position: absolute;
  top: 16px;
  right: 16px;
  display: inline-flex;
  gap: 2px;
  padding: 2px;
  border-radius: 9999px;
  background: #f1f5f9;
}
.langToggleBtn,
.langToggleActive {
  border: none;
  cursor: pointer;
  padding: 4px 10px;
  border-radius: 9999px;
  font-size: 12px;
  font-weight: 700;
  color: #64748b;
  background: transparent;
}
.langToggleActive {
  background: #ffffff;
  color: #0f766e;
  box-shadow: 0 1px 2px rgb(0 0 0 / 0.08);
}
.langToggleBtn:disabled,
.langToggleActive:disabled {
  opacity: 0.6;
  cursor: default;
}
```

(`.loginCard` cần `position: relative` — kiểm tra, thêm nếu chưa có.)

- [ ] **Step 4: Login page dùng dict**

Trong `page.tsx`: thêm `const { lang, dict } = useN400Lang();` + `const t = dict.login;` đầu component (import từ `@/lib/n400/i18n/provider`). `<LangToggle />` render ngay đầu `styles.loginCard` (trước `cardHeader`). Mapping thay chuỗi (2 chỗ tagline mobile+desktop, 2 chỗ headline, v.v.):

| Chuỗi cứng hiện tại | Thay bằng |
|---|---|
| `TỰ TIN CHINH PHỤC{'\n'}GIẤC MƠ MỸ!` (×2) | `{t.tagline}` (giữ white-space CSS; render với `whiteSpace: 'pre-line'` đã có sẵn qua CSS brandTagline — verify) |
| `Học thông minh.` (×2) | `{t.headline1}` |
| `Đậu phỏng vấn.` (×2) | `{t.headline2}` |
| `Trở thành công dân Mỹ.` (×2) | `{t.headline3}` |
| heroSub 2 dòng desktop | `{t.heroSub}` (bỏ `<br />`, để CSS wrap) |
| mobileHeroSub 3 dòng | `{t.mobileHeroSub}` (bỏ `<br />`) |
| socialProofText | `{t.socialProof}` |
| `Welcome to` trong cardTitle | `{t.welcomePrefix}` (giữ `<span>` N400 Ready) |
| cardSubtitle | `{t.cardSubtitle}` |
| `Tiếp tục với Google/Apple/Facebook` | PROVIDERS bỏ field `label`, render `{t.continueWith} {providerName}` với `providerName` = 'Google'/'Apple'/'Facebook' (thêm field `name`) |
| `HOẶC` | `{t.or}` |
| `Tiếp tục với Email` | `{t.continueWithEmail}` |
| placeholder `Email` / `Mật khẩu` | `{t.emailPlaceholder}` / `{t.passwordPlaceholder}` |
| `Đang đăng nhập...` / `Đăng nhập` | `{t.signingIn}` / `{t.signIn}` |
| `Quên mật khẩu?` / `Tạo tài khoản` | `{t.forgotPassword}` / `{t.createAccount}` |
| `Đăng nhập thất bại. Vui lòng thử lại.` | `{t.loginFailed}` |
| securityNote 2 dòng | `{t.securityNote1}<br />{t.securityNote2}` |
| `href="/vi/forgot-password"` / `href="/vi/signup"` | `` href={`/${lang}/forgot-password`} `` / `` href={`/${lang}/signup`} `` |

PROVIDERS mới:

```ts
const PROVIDERS: { id: OAuthId; name: string; Icon: () => React.ReactElement }[] = [
  { id: 'google', name: 'Google', Icon: GoogleIcon },
  { id: 'apple', name: 'Apple', Icon: AppleIcon },
  { id: 'facebook', name: 'Facebook', Icon: FacebookIcon },
];
```

và trong render: `<span>{t.continueWith} {name}</span>`.

- [ ] **Step 5: Verify + commit**

`npx tsc --noEmit`, `npx vitest run`, và smoke bằng dev server:

```bash
curl -s http://localhost:3789/n400ready/login | grep -o "Đậu phỏng vấn" | head -1   # bản vi (không cookie)
curl -s -H "Cookie: n400_lang=en" http://localhost:3789/n400ready/login | grep -o "Pass the interview" | head -1   # bản en
```

(Nếu không có server sẵn ở 3789, tự chạy `npx next dev -p 3102` rồi curl port đó, xong kill.)

```bash
git add "src/app/n400ready/(auth)" src/lib/n400/i18n 2>/dev/null; git add -u
git commit -m "feat(n400): bilingual login page with VI/EN toggle"
```

---

### Task 5: Popup chọn ngôn ngữ lần đầu

**Files:**
- Create: `src/components/n400/LanguageSelectModal.tsx`
- Modify: `src/app/n400ready/(app)/layout.tsx`

- [ ] **Step 1: Modal component**

```tsx
'use client';

import { useState, useTransition } from 'react';
import { useRouter } from 'next/navigation';
import type { N400Lang } from '@/lib/n400/i18n/config';
import { setN400Language } from '@/lib/n400/i18n/actions';

/**
 * First-login language chooser. Rendered only while
 * n400_user_profile.ui_language IS NULL; confirming writes DB + cookie so
 * it never shows again. Deliberately bilingual and not dismissible.
 */
export function LanguageSelectModal() {
  const router = useRouter();
  const [choice, setChoice] = useState<N400Lang>('vi');
  const [pending, startTransition] = useTransition();

  function confirm() {
    if (pending) return;
    startTransition(async () => {
      await setN400Language(choice);
      router.refresh();
    });
  }

  const options: { code: N400Lang; label: string; sub: string; flag: string }[] = [
    { code: 'vi', label: 'Tiếng Việt', sub: 'Giao diện tiếng Việt', flag: '🇻🇳' },
    { code: 'en', label: 'English', sub: 'English interface', flag: '🇺🇸' },
  ];

  return (
    <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-900/50 p-4">
      <div className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-xl">
        <h2 className="text-lg font-bold text-gray-800">Chọn ngôn ngữ hiển thị</h2>
        <p className="mt-0.5 text-sm text-gray-500">Choose your display language</p>

        <div className="mt-5 space-y-3">
          {options.map((opt) => (
            <button
              key={opt.code}
              type="button"
              onClick={() => setChoice(opt.code)}
              aria-pressed={choice === opt.code}
              className={`flex w-full items-center gap-3 rounded-xl border-2 p-4 text-left transition ${
                choice === opt.code
                  ? 'border-teal-500 bg-teal-50'
                  : 'border-gray-200 bg-white hover:border-gray-300'
              }`}
            >
              <span className="text-2xl">{opt.flag}</span>
              <span className="flex-1">
                <span className="block font-semibold text-gray-800">{opt.label}</span>
                <span className="block text-xs text-gray-500">{opt.sub}</span>
              </span>
              {choice === opt.code && <span className="font-bold text-teal-600">✓</span>}
            </button>
          ))}
        </div>

        <button
          type="button"
          onClick={confirm}
          disabled={pending}
          className="mt-5 w-full rounded-xl bg-teal-600 py-3 font-semibold text-white hover:bg-teal-700 disabled:opacity-60"
        >
          {pending ? '…' : 'Xác nhận / Confirm'}
        </button>
        <p className="mt-3 text-center text-xs text-gray-400">
          Bạn có thể đổi lại trong phần Tài khoản · You can change this later in Account
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: (app) layout query + render modal**

`src/app/n400ready/(app)/layout.tsx` thành async, thêm truy vấn `ui_language` bằng server client (pattern y hệt `setup/actions.ts` — createServerClient + cookies, KHÔNG setAll):

```tsx
import type { ReactNode } from 'react';
import { cookies } from 'next/headers';
import { createServerClient } from '@supabase/ssr';
import { MobileNav, Sidebar } from '@/components/n400/Sidebar';
import { Header } from '@/components/n400/Header';
import { RegisterSW } from '@/components/n400/RegisterSW';
import { LanguageSelectModal } from '@/components/n400/LanguageSelectModal';
import { Suspense } from 'react';

/**
 * Authenticated app layout — sidebar, header, mobile-nav.
 * Only pages in the (app) route group get this chrome.
 * Also decides whether the first-login language modal must show
 * (ui_language IS NULL — user has never chosen).
 */
export default async function N400AppChromeLayout({ children }: { children: ReactNode }) {
  const cookieStore = await cookies();
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll: () => cookieStore.getAll(), setAll: () => {} } }
  );
  const {
    data: { user },
  } = await supabase.auth.getUser();
  let needsLanguageChoice = false;
  if (user) {
    const { data } = await supabase
      .from('n400_user_profile')
      .select('ui_language')
      .eq('user_id', user.id)
      .maybeSingle();
    needsLanguageChoice = data !== null && data.ui_language === null;
  }

  return (
    <div className="flex h-dvh overflow-hidden bg-slate-50 font-sans text-gray-900">
      <RegisterSW />
      {needsLanguageChoice && <LanguageSelectModal />}
      <Sidebar />
      <div className="flex min-w-0 min-h-0 flex-1 flex-col overflow-hidden lg:ml-64">
        <Suspense fallback={<div className="h-16 lg:h-20" />}>
          <Header />
        </Suspense>
        <main className="page-transition relative z-0 flex-1 overflow-y-auto px-4 pb-28 pt-4 sm:px-6 lg:p-8">
          {children}
        </main>
      </div>
      <MobileNav />
    </div>
  );
}
```

(`data !== null` — user chưa có row (đang ở /setup) thì KHÔNG hiện popup; setup xong vào app mới hiện.)

- [ ] **Step 3: Verify + commit**

`npx tsc --noEmit`, `npx vitest run` sạch. Manual check cần login thật — để Task 7 verify tổng bằng SQL toggle. Commit:

```bash
git add src/components/n400/LanguageSelectModal.tsx "src/app/n400ready/(app)/layout.tsx"
git commit -m "feat(n400): first-login language chooser modal"
```

---

### Task 6: Profile edit — select ngôn ngữ có tác dụng thật

**Files:**
- Modify: `src/app/n400ready/(app)/profile/edit/page.tsx`

- [ ] **Step 1: Repoint select sang ui_language**

Trong `ProfileEditForm`:
1. Bỏ `preferred_language: language,` khỏi payload `updateProfile` (không ghi bảng chung nữa).
2. Khởi tạo state từ cookie thay vì `profile.preferred_language`:

```ts
import { DEFAULT_N400_LANG, N400_LANG_COOKIE, isN400Lang, type N400Lang } from '@/lib/n400/i18n/config';
import { setN400Language } from '@/lib/n400/i18n/actions';

function readLangCookie(): N400Lang {
  if (typeof document === 'undefined') return DEFAULT_N400_LANG;
  const raw = document.cookie
    .split('; ')
    .find((c) => c.startsWith(`${N400_LANG_COOKIE}=`))
    ?.split('=')[1];
  return isN400Lang(raw) ? raw : DEFAULT_N400_LANG;
}
```

```ts
const [language, setLanguage] = useState<N400Lang>(readLangCookie);
```

3. Trong `handleSave`, sau khi `updateProfile` thành công và trước `router.push`:

```ts
await setN400Language(language);
```

4. `router.push('/n400ready/profile')` giữ nguyên — cookie đã đổi nên trang mới render theo ngôn ngữ mới (thấy rõ từ Đợt 3 khi profile được dịch; Đợt 2 hiệu ứng thấy được là `<html lang>` đổi).

- [ ] **Step 2: Verify + commit**

`npx tsc --noEmit`, `npx vitest run`, `npx eslint "src/app/n400ready/(app)/profile/edit/page.tsx"` sạch.

```bash
git add "src/app/n400ready/(app)/profile/edit/page.tsx"
git commit -m "feat(n400): profile language select drives ui_language + cookie"
```

---

### Task 7: Verify tổng + build

- [ ] **Step 1:** `npx vitest run` (toàn bộ), `npx tsc --noEmit`, `npx eslint src` — sạch.
- [ ] **Step 2:** `npx next build 2>&1 | tail -15` — pass.
- [ ] **Step 3:** Smoke có server (dev sẵn có hoặc tự chạy port 3102):
  - `curl -s /n400ready/login` → chứa `Đậu phỏng vấn` và `lang="vi"`.
  - `curl -s -H "Cookie: n400_lang=en" /n400ready/login` → chứa `Pass the interview` và `lang="en"`.
  - `curl -sI /n400ready/practice` (không session) → 307 về `/n400ready/login` (guard còn nguyên).
- [ ] **Step 4:** Báo controller kết quả — KHÔNG tự merge. Controller sẽ chạy review + quyết định merge.

## Definition of Done (Đợt 2)

- Cột `ui_language` tồn tại trên Supabase (nullable, CHECK vi/en).
- Login mặc định vi, toggle EN đổi toàn bộ chữ trang login ngay, `<html lang>` đổi theo.
- User có `ui_language NULL` vào app thấy popup; xác nhận xong không bao giờ thấy lại; user đang /setup chưa thấy popup.
- Select Ngôn ngữ trong Profile ghi `ui_language` + cookie; không ghi `profiles.preferred_language` nữa.
- 6 commit atomic như trên; vitest/tsc/eslint/build đều pass.
